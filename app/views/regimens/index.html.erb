<h1>レジメン一覧</h1>
<%= link_to 'レジメン新規登録', new_regimen_path %>
<br>
<h3>レジメン一覧</h3>
<div class="regimens">
  <% @regimens.each do |regimen| %>
    <div class="regimen">
      <%= link_to "詳細", regimen_path(regimen.id) %>
      <a>　</a>
      <a class="regimen-name"><%= regimen.name %></a>
    </div>
  <% end %>
</div>
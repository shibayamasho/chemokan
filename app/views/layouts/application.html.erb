<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>けもかん</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if user_signed_in? %>
      <header>
        <div class="header-left">
          <%= link_to root_path do %>
            <%= image_tag "logo.png", alt: "けもかんロゴ", class: "header-logo-image" %>
          <% end %>
        </div>
        <div class="header-right">
          <ul class="header-right-lists">
            <li class="current-user-name">【<%= current_user.name %>さんがログイン中】</li>
            <li class="sign-out"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
          </ul>
        </div>
      </header>
    <% end %>
      
    <div class="contents-wrapper">
      <% if user_signed_in? %>
        <nav>
          <ul>
            <li class="link-to-top"><%= link_to "TOP(患者一覧)", root_path %></li>
            <li class="link-to-plans"><%= link_to "レジメン一覧", plans_path %></li>
          </ul>
        </nav>
      <% end %>

      <main>
        <%= yield %>
      </main>
    </div>

  </body>
</html>
